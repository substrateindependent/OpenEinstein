# Architecture

This document tracks the live architecture of the repository.

## Current State (Bootstrap)

- Python package scaffold with subsystem directories in `src/openeinstein/`
- Typer CLI entrypoint with command groups (`run`, `pack`, `approvals`)
- Policy and config scaffolding in `configs/`
- Canonical documentation index and core architecture doc in `docs/canonical/`

## Planned Evolution

Implementation follows phased delivery described in `OpenEinstein-Implementation-Plan.md`.
